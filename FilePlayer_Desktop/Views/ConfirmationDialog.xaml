<UserControl x:Class="FilePlayer.Views.ConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="400">

    <Grid>
        <StackPanel>
            <TextBlock Margin="10,100,10,10" FontSize="20" TextWrapping="Wrap" Grid.Row="1" Grid.Column="1">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}Would you like to open '{0}'?">
                        <Binding Path="Content"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>

            <StackPanel x:Name="buttonPanel" Orientation="Horizontal">
                <Button x:Name="noButton" AutomationProperties.AutomationId="CustomPopupNoButton" Margin="50" HorizontalAlignment="Right" Width="100" Height="40" Click="Button_Click">No</Button>
                <Button x:Name="yesButton" AutomationProperties.AutomationId="CustomPopupYesButton" Margin="50" HorizontalAlignment="Right" Width="100" Height="40" Click="Button_Click">Yes</Button>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
